# WARS-Birdhouse-Console
Python console apps for the WARS birdhouse mesh network project

Create the virtual environment:

      python3 -m venv dev
      . dev/bin/activate
      pip install -r requirements.txt
